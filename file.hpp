#include <string>


extern void createDirectories(const std::string &path);
extern std::string directoryOf(const std::string &path);
extern std::string baseOf(const std::string &path);
extern bool fileExists(const std::string &path);
